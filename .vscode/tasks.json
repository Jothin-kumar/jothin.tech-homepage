{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "wrangler-dev",
            "type": "shell",
            "command": "./build.sh && wrangler pages dev build-output",
            "problemMatcher": []
        }
    ]
}